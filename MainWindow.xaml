<Window x:Class="ImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageViewer"
        mc:Ignorable="d"
		AllowsTransparency="True"
		ResizeMode="CanResize"
        WindowStyle="None"
        Title="MainWindow" 
		MouseDown="Window_MouseDown"
		KeyDown="Window_KeyDown"
		Height="450"
		Width="800">
	<WindowChrome.WindowChrome>
		<WindowChrome 
            CaptionHeight="0"
            ResizeBorderThickness="5" />
	</WindowChrome.WindowChrome>
	<Window.ContextMenu>
		<ContextMenu>
			<MenuItem Header="선택 이미지 클립보드 인자 복사" Click="SaveArgClipboardSingle"/>
			<MenuItem Header="모든 이미지 클립보드 인자 복사" Click="SaveArgClipboardAll"/>
			<MenuItem Header="상단 고정(토글)" Click="Go_Topmost"/>
			<MenuItem Header="오토 리사이징" Click="Go_AutoResizing"/>
		</ContextMenu>
	</Window.ContextMenu>
	<Grid AllowDrop="True"
          Drop="DropImageFile"
		  Background="LightGray">

		<TextBlock FontSize="32" Text="이미지를 드래그 드랍 해주세요." VerticalAlignment="Center" HorizontalAlignment="Center">
			
		</TextBlock>
		<Image x:Name="DisplayedImage" Stretch="Fill" />
	</Grid>
</Window>
